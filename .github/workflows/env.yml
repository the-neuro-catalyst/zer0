# .github/workflows/env.yml
# This file centralizes environment variables used across GitHub Actions workflows.

env:
  RUST_VERSION: "1.85.0"
  NODE_VERSION: "20"
  PNPM_VERSION: "9"
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
